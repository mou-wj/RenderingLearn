{
  "name": "VulkanSDK",

  "type": "external",

  "source": null,

  "need_compile": false,

  "root": "${ENV:VULKAN_SDK}",

  "install_layout": {
    "include": "${ENV:VULKAN_SDK}/Include",
    "lib": "${ENV:VULKAN_SDK}/Lib"
  },

  "dependencies": [],

  "auto_generate_find_cmake": false,

  "notes": "Vulkan SDK (LunarG) is NOT pulled by BuildDeps. Install manually or via package manager. Use VULKAN_SDK environment variable to point to installation. No build steps; CMake expects find_package(Vulkan) to succeed."
}